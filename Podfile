# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HSERemoteAccelerometer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HSERemoteAccelerometer
  
  pod 'RxSwift', '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'
  pod 'Charts'
  pod 'ObjectMapper', '~> 2.2'
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftDateTools'
  pod 'DTAlertViewContainer', '~> 0.1.8'
  #pod 'DTAlertViewContainer', :path => '~/Documents/Libraries/DTAlertViewContainer'
  
  target 'HSERemoteAccelerometerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HSERemoteAccelerometerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
